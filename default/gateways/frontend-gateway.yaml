kind: Gateway
apiVersion: gateway.networking.k8s.io/v1beta1
metadata:
  name: team-whereami
  namespace: team-whereami
  annotations:
    networking.gke.io/certmap: whereami-cert-map
spec:
  gatewayClassName: gke-l7-global-external-managed-mc
  listeners:
  - name: https # list the port only so we can redirect any incoming http requests to https
    protocol: HTTPS
    port: 443
    allowedRoutes:
      kinds:
      - kind: HTTPRoute
  addresses:
  - type: NamedAddress
    value: whereami-ip
